# Домашнее задание к лекции 2.2  
## «Regular expressions»

### 📌 Описание
Данная программа приводит в порядок адресную книгу, содержащую контактные данные сотрудников.  
Исходные данные содержат дубли, некорректно записанные ФИО и телефоны.

Задача — нормализовать данные с использованием стандартных средств Python и регулярных выражений.

---

### 📂 Структура данных
Каждая запись в исходном файле `phonebook_raw.csv` имеет следующий формат:

lastname, firstname, surname, organization, position, phone, email

---

### 🎯 Задачи, реализованные в программе

1. **Приведение ФИО к единому виду**
   - Фамилия, Имя и Отчество распределяются по отдельным полям
   - Используется объединение и разбиение строк без регулярных выражений

2. **Нормализация телефонных номеров**
   - Все номера приводятся к формату:
     ```
     +7(999)999-99-99
     ```
   - Добавочные номера сохраняются в формате:
     ```
     +7(999)999-99-99 доб.9999
     ```
   - Используются регулярные выражения

3. **Объединение дублирующихся записей**
   - Если совпадают Фамилия и Имя — записи считаются относящимися к одному человеку
   - Данные объединяются в одну запись

---

### 🛠 Используемые технологии
- Python 3
- Модуль `csv`
- Модуль `re` (regular expressions)

---

### 📄 Результат
- Обработанные данные сохраняются в файл `phonebook.csv`
- Все контакты приведены к единому, корректному формату
